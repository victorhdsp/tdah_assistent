version: "3.1"

intents:
  - criar_lembrete

entities:
  - data
  - hora
  - local
  - acao
  - objeto
  - evento
  - prazo
  
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
